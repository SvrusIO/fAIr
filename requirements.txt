# Managed via pip-compile.
# To update, run:
#   pip-compile --extra training --extra monitoring --extra adapters \
#       --output-file=requirements.txt requirements-dev.in
#
# Developers without pip-tools can install the unpinned inputs instead:
#   pip install -r requirements-dev.in
#
# See README.md for platform-specific torch installation guidance.
-r requirements-dev.in