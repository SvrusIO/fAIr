# ReadTheDocs configuration file
# Alternative to GitHub Pages for documentation hosting

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs/conf.py
  builder: html

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
      extra_requirements:
        - dev

formats:
  - htmlzip
  - pdf
